name: Deploy

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build and push images
        run: echo "Build/push steps go here"
      - name: Terraform apply
        run: echo "Terraform apply goes here"
