name: Deploy

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build and push images
        run: |
          echo "ERROR: Deploy workflow is a stub. Implement build/push steps before using this workflow."
          exit 1
      - name: Terraform apply
        run: |
          echo "ERROR: Deploy workflow is a stub. Implement Terraform apply steps before using this workflow."
          exit 1
