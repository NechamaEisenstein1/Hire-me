name: Deploy (stub)

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Stub notice
        run: |
          echo "This workflow is intentionally a stub and currently disabled."
          echo "Implement real build/push/apply steps before enabling production deploys."
